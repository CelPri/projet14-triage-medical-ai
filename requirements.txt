fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pytest==8.3.4
httpx==0.28.1
mlflow==2.19.0

# vLLM est utilisé seulement sur le serveur GPU de déploiement.
# Il n'est pas installé dans la CI ni sur Windows.
# Commande prévue sur serveur Linux GPU :
# pip install vllm==0.6.6.post1

